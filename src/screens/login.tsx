import React from "react";
import { View, Text, StyleSheet, TextInput, Button } from "react-native";
import { Colors, Spacing, FontSizes } from "../assets/styles/globalStyles";

const LoginScreen = () => {
  return (
    <View>
      <Text>Login</Text>
      <TextInput />
      <TextInput />
    </View>
  );
};

export default LoginScreen;
